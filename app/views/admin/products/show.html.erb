<% content_for(:page_styles) { %>
  <%= stylesheet_link_tag "pages/admin", "data-turbo-track": "reload" %>
<% } %>

<%= render "shared/admin_nav" %>

<main>
  <section class="admin-header">
    <h1><%= @product.name %></h1>
    <div class="btn-group">
      <%= link_to "Редактировать", edit_admin_product_path(@product), class: "btn btn-warning" %>
      <%= link_to "Удалить", admin_product_path(@product), method: :delete, data: { turbo_method: :delete, turbo_confirm: "Вы уверены?" }, class: "btn btn-danger" %>
      <%= link_to "Назад", admin_products_path, class: "btn btn-secondary" %>
    </div>
  </section>

  <div class="product-details">
    <div class="detail-row">
      <div class="detail-label">Название:</div>
      <div class="detail-value"><%= @product.name %></div>
    </div>

    <div class="detail-row">
      <div class="detail-label">Категория:</div>
      <div class="detail-value"><%= @product.category&.name || "Не указана" %></div>
    </div>

    <div class="detail-row">
      <div class="detail-label">Цена:</div>
      <div class="detail-value"><strong>$<%= @product.price %></strong></div>
    </div>

    <div class="detail-row">
      <div class="detail-label">Количество:</div>
      <div class="detail-value"><%= @product.quantity %></div>
    </div>

    <div class="detail-row">
      <div class="detail-label">Описание:</div>
      <div class="detail-value"><%= simple_format(@product.description) %></div>
    </div>

    <% if @product.image.attached? %>
      <div class="detail-row">
        <div class="detail-label">Изображение:</div>
        <div class="detail-value">
          <%= image_tag @product.image, class: "product-preview-img" %>
        </div>
      </div>
    <% end %>
  </div>
</main>
